//document.addEventListener('DOMContentLoaded', function() {

    // --- SMOOTH SCROLL PARA LINKS DA NAVEGAÇÃO ---
   // const navLinks = document.querySelectorAll('.main-nav a, .cta-button[href^="#"]');

    //navLinks.forEach(link => {
    //    link.addEventListener('click', function(e) {
    //        e.preventDefault();
    //        let targetId = this.getAttribute('href');
    //        let targetElement = document.querySelector(targetId);
            
    //        if(targetElement) {
    //            targetElement.scrollIntoView({
    //                behavior: 'smooth',
    //                block: 'start'
    //            });
    //        }
     //   });
    //});

    // --- MANIPULAÇÃO DO FORMULÁRIO DE CONTATO ---
   // const contactForm = document.getElementById('contact-form');

    //contactForm.addEventListener('submit', function(e) {
    //    e.preventDefault(); // Impede o envio real do formulário

        // Aqui você pode adicionar a lógica para enviar os dados
        // para um serviço como Netlify Forms, Formspree, ou um backend próprio.
        
        // Exemplo: Criando um objeto com os dados do formulário
    //    const formData = new FormData(contactForm);
    //    const data = Object.fromEntries(formData.entries());
    //    console.log("Dados do formulário:", data);

        // Feedback para o usuário
    //    alert('Obrigado pelo seu contato! Retornaremos em breve.');
        
        // Limpa o formulário após o envio
    //    contactForm.reset();
//    });

//});